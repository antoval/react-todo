//code is removed because require is not defined client side, which breaks the app when deployed.

//var path = require('path');
//
//module.exports = function(eyeglass, sass) {
//  return {
//    sassDir: path.join(__dirname, 'stylesheets')
//  }
//}